import styles from './LogoTitle.module.css';

export const LogoTitle = () => {
    return (
        <div className={styles.container}>
            <span className={styles.title}>MyPlayer</span>
        </div>
    )
}